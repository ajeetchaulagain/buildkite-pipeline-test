env:
  TEST_ENV_VARIABLE: 'Hello World'
steps:
  - label: ":hammer: Install"
    command: "npm install"
    key: install

  # - block: "Release"
  #   label: 'Run playwright tests'
  #   prompt: "Fill out the details for release"
  #   fields:
  #     - text: "Release Name"
  #       key: "release-name"

  # - command: "echo ${ENV_VARIABLE}"

    
  - label: Print env variables - ${TEST_ENV_VARIABLE}"
    command: '.buildkite/scripts/run-test.sh "${TEST_GROUP}"'
    env:
      ENV_VARIABLE: 'Hello'
